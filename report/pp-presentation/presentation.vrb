\frametitle{The Struct}

\begin{lstlisting}[caption=First 2d example]

struct linked_struct {
    int up;
    int down;
    int left;
    int right;
    float value[N][M];
    float old_value[N][M];
    char inside[N][M];
};
\end{lstlisting}
